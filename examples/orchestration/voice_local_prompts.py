"""Prompts for voice_local.yaml.

The local voice agent uses an inline ``system_prompt`` in the manifest, so this
registry is intentionally empty. Add named templates here if you switch the
persona to a ``prompt:`` reference.
"""

PROMPTS: dict = {}
